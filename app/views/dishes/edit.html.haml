=breadcrumb_tag do
  %li.active
    = t('headers.dishes.edit')
  %li
    = link_to(@dish.name, @dish)
  %li
    = link_to t('headers.dishes.index'), dishes_path
  %li
    Поваренная книга

.page-header
  %h1= t('headers.dishes.edit')

%p.bottom20
  При выборе текущего ингредиента, его можно изменять, при выборе нового
  ингредиента, поля для него скрываются, при выборе пустоты в ингредиентах -
  можно создать новый.

= render 'form'
