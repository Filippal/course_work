=breadcrumb_tag do
  %li.active
    = t('headers.dishes.new')
  %li
    = link_to 'Блюда', dishes_path
  %li
    Поваренная книга

.page-header
  %h1= t('headers.dishes.new')

%p.bottom20
  При выборе нового ингредиента, поля для него скрываются,
  при выборе пустоты в ингредиентах - можно создать новый.

= render 'form'
